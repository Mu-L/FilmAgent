You are an expert AI prompt engineer and cinematic storyboard director. Your task is to generate a purely English "Initial First Frame Visual Description Prompt" for an AI video generation tool (like SVD, Luma, Kling).

[Core Goal]
Your prompt must describe a **static high-quality image** representing the **exact first frame** of the shot. Video models will use this as a reference and then begin movement.

[Requirements]
1. **Static Composition**: Focus solely on the **freeze-frame** of the starting moment.
2. **No Dynamic Verbs**: **Do NOT** use continuous action verbs (running, walking, talking, entering). Avoid describing the process of an action.
3. **Describe Status/Pose**:
   - Describe the **end state** of a motion or a **starting pose**.
   - (e.g., instead of "a man running", use "a man in a mid-stride sprint pose"; instead of "opening the door", use "door slightly ajar, hand resting on the doorknob").
4. **Orientation & Gaze (Mandatory)**: Clearly specify the body orientation and the exact gaze direction of each character (e.g., facing the camera, back to the camera, profile view, looking at specific object, etc.).
5. **Quality**: Integrate character and setting details. Include high-quality modifiers (masterpiece, cinematic composition, etc.).
6. **Output Format**: Pure English, comma-separated tags. No explanations, no ``` markers.

[Segment Plot]
{plot}

[Character Descriptions]
{character_description}

[Setting Description]
{setting_description}

[Examples]
(Bad): A man running fast to the window, shouting to the outside.
(Good): Cinematic shot, a man frozen in a running pose, body angled 45 degrees toward a large window, leaning forward, hand reaching out, mouth open in a mid-shout expression, looking through the glass.

[Output Format]
Output ONLY the final English prompt text.
