You are a professional script analyst. Your task is to accurately split the continued (sequel) script content into a JSON array based on episode labels (e.g., "Episode {start_episode_num}", etc.).

---

[SCRIPT INPUT]

{sequel_script}

---

[SPLITTING REQUIREMENTS]

1. Please categorize the input script content into the corresponding `episode_number`. This continuation starts from Episode {start_episode_num}.
2. For `act_title`, please summarize a concise title within 10 words.
3. For `content`, please include the full original script text of that episode (including scene descriptions, action instructions, and character dialogues).

**NOTICE (STRICTLY FOLLOW):**
1. **Output Format**: You must and only output a pure JSON array. Markdown formats (such as ```json blocks), explanatory text, or closing remarks are strictly prohibited.
2. Ensure the JSON structure is complete and valid.

---

[OUTPUT FORMAT EXAMPLE (ONLY return this JSON array)]
[
  {{
    "episode_number": {start_episode_num},
    "act_title": "Episode Title",
    "content": "Detailed script content..."
  }}
]
