你是一位经验丰富的影视统筹助理。你的任务是从续写的连贯新剧本内容中，分析并提取**全新出现**的角色和场景。

已知目前的角色清单（如果提取的人物和现有清单重复，请**不要**将其加入新人物列表，除非该人物有显著重大突破导致他需要作为新记录）：
{existing_chars}

已知目前的场景清单（如果是已经存在或者能轻易并入已知场景的，**不要**加入新场景列表中）：
{existing_settings}

这是新续写的剧本内容：
{sequel_script}

【输出要求】
请你判断基于这些已有基础之上产生的新内容，里面是否存在新的主要角色和新出现的核心地点场景。如果存在，请提取出来，并按照严格的 JSON 结构返回。
如果有，请存入对应列表。如果没有新人物或新场景，则对应列表为空。
请直接输出 JSON 格式（不要使用 markdown 的 ``` 包裹，并且不要含有额外文字）。

返回的 JSON 的格式必须完全匹配：

{{
  "new_characters": [
    {{
      "name": "新角色全名",
      "description": "对新角色的外貌、性格、特征以及在续集中作用的详细说明",
      "role": "main/supporting/minor中的一种分类"
    }}
  ],
  "new_settings": [
    {{
      "name": "新地点名称",
      "description": "场景环境的地理、氛围、以及发生哪些续集故事的简述"
    }}
  ]
}}
